export interface AboutData {
  name: string;
  bio: string;
  skills: string[];
}

export interface Project {
  title: string;
  description: string;
  url: string;
  imageUrl: string;
  tags: string[];
}
